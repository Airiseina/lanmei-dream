# 贡献准则

感谢你对 lanmei-dream（蓝妹）的关注。在提交 issue、pull request 或以任何形式添加代码之前，请先阅读本指南。

* * *

## 目录

1. 简介与核心原则
2. 开发环境与代码规范
3. 提交约定
4. AI 贡献准则
5. 贡献描述真实性要求
6. Pull Request 与 issue 规范
7. 代码评审与合并流程

* * *

## 1. 简介与核心原则

lanmei-dream 是一个用 Go 编写的跨平台 AI 聊天机器人，由重庆邮电大学蓝山工作室开发和维护。项目的核心思想——多平台协议兼容、常驻服务的稳定性、LLM 链路可观测与可治理——决定了我们如何接受贡献。请始终牢记以下原则：

- **稳定性优先。** 这是一个 7×24 住在 IM 群里的服务。你的改动不应破坏网关协议兼容、消息不丢、不引入未处理的阻塞路径；涉及 LLM 调用的地方要考虑超时、降级与配额。
- **清晰胜过取巧。** 请写出新贡献者无需调试器就能读懂的代码，优先直截了当、明确的实现，避免一次性抽象与过度追求精巧的技巧。
- **评审要讲证据。** 讨论与评审应基于具体的事实、度量与引用，而非主观印象与语气。
- **尊重他人的时间。** 贡献是协作性的工作。请保持沟通简洁、建设性、专业（参见第 4、5、6 节）。

每一份贡献，无论完全由自然人编写、由 AI 工具辅助，还是以电子方式生成，都受本文档各项标准的约束。

* * *

## 2. 开发环境与代码规范

所有代码必须符合以下规则。CI 流水线（`.github/workflows/go.yml`）会在每次 push 与 pull request 时自动执行静态检查、构建与测试；但在提交前请在本地先行通过。

### 2.1 必需工具链

- **Go 1.26** 工具链（CI 锁定 1.26.5）。
- 涉及管理面板前端（`manager/`）的改动需要 **Node.js + npm**。

### 2.2 格式化（gofmt）

- 所有 Go 代码必须通过 `gofmt`，并使用仓库既有风格；提交前执行 `gofmt -l .` 应输出为空。
- 前端代码遵循 `manager/` 下既有配置与代码风格，不要引入第二种格式化风格。

### 2.3 静态检查（go vet）

`go vet` 必须以 **零告警** 通过：

```
go vet ./...
```

除非有清晰、可记录的理由，否则不要通过 `//nolint` 之类的手段静默告警。

### 2.4 注释

- 注释必须 **准确、简洁**，可以使用 **中文或英文**，并与所在文件、所在模块的既有风格保持一致。
- 当代码的"是什么"并不一目了然时，请说明"为什么"。不要复述代码。
- 让注释紧跟其描述的代码，并随代码一同更新。

### 2.5 输出与用户可见内容

- 日志、错误与诊断信息等 **面向运维者** 的输出，应清晰、准确；语言可使用中文或英文，同一模块内应保持一致，并遵循既有代码的惯例。
- 聊天文案（prompts、技能、插件回复等）**面向最终用户**，是用户体验的一部分：允许并鼓励使用 emoji 与活泼的表达，但请与项目既有语料风格保持一致，且不得影响功能正确性。

### 2.6 模块划分

- 请沿功能边界组织 `internal/` 下的包（`gateway`、`plugin`、`kb`、`manager` 等），按职责分组。
- **不要** 把所有类型都堆叠在一个文件中，也不要新建与既有分层平行的第二套结构。
- 插件贡献请遵循对应规范：内置业务插件见 `internal/bizplugin/README.md`；Wasm 插件见 `PLUGIN_DEVELOPMENT.md` 与 `schema/plugin/lanmei-plugin.wit`。

### 2.7 测试

- 合入前，PR 必须通过全部测试：

```
go test ./...
```

- 测试文件命名为 `*_test.go`，与被测代码同目录；新增行为请尽量附带可运行的测试，涉及协议适配、权限判定、RAG 召回等关键链路时尤为如此。

### 2.8 提交前的本地检查清单

```
go vet ./...
go build ./...
go test ./...
```

以上三项必须全部通过，它们与 CI 的步骤一致；改动管理面板前端时，另需保证：

```
cd manager && npm install && npm run build
```

可以通过。在本地通过即意味着你的分支已具备 CI 就绪条件。

* * *

## 3. 提交约定

- **撰写清晰的提交信息。** 以简要的主题行开头，说明 _改了什么_ 与 _为什么改_；仅在必要时补充正文，解释动机与取舍。请遵循仓库既有历史的风格。
- **提交连贯、可评审的单元。** 一次提交应自成一体、逻辑清晰。不要把单个变更拆分成大量琐碎、嘈杂的提交，也不要把不相关的改动塞进同一次提交。涉及纯格式的改动尽量单独提交。
- **不要提交无关文件。** 切勿加入无关的二进制、生成产物或本机本地文件，保持仓库整洁。
- **切勿提交敏感信息。** 本项目配置走 `.env`（API Key、数据库凭据、管理面板密钥等）。绝不提交 `.env`、令牌、密钥或任何敏感材料；新增配置项请只更新 `.env.example` 中的模板与文档说明。暂存前请再次确认未包含任何机密文件。

* * *

## 4. AI 贡献准则

我们不反对在开发过程中借助 AI 工具作为辅助，本项目自身也大量应用 AI 技术。但 AI 辅助会改变一份贡献被评审与治理的方式。以下规则具有约束力。

> **署名与责任须归于自然人。** 任何涉及 AI 参与的代码，最终都必须由 **提交它的自然人开发者** 负责。任何贡献的核心永远是自然人开发者；AI 仅是其编写阶段的辅助工具。该开发者对贡献代码的正确性、许可证合规与后续维护承担 **全部责任**——包括逐行理解并亲自核验 AI 产出的内容。

- **禁止 AI Agent 自行运营账号，禁止 AI 作为 commit author 或 co-author。** 严禁以 **非自然人所有** 的账号（例如以 AI Agent 自身的名义运营的账号，或冒用、顶替自然人身份）提交代码、开启 pull request 或进行署名。AI 只是工具，**不得** 署名，也不得冒充贡献者。Bot 账号仅限维护者用于自动化用途。

- **提交前必须亲自审查 AI 产出。** 你提交的每一行代码、每一段 issue/PR 描述，都必须经你本人阅读、理解并验证。如果你无法解释一段由 AI 生成的代码为什么这样写，就不要提交它。把未审查的 AI 输出原样转发到本仓库，等同于由 AI 替你署名。

- **AI 编写代码的风格须一致。** AI 辅助编写的代码应与既有代码保持一致性——格式、包划分、命名均如此，与自然人编写的代码一视同仁。

- **禁止低信息密度的表述。** AI 生成的内容不得添加通篇空泛、信息密度低的水分，不得出现冗长的流水账式记录或辞藻冗余的铺陈。文档、注释与 issue 文本都应精炼，聚焦于 **结论** 而非得出结论的 **过程**。

### 4.1 垃圾贡献的界定与处理

我们注意到，AI 时代的低质量贡献正大量消耗维护者的时间。以下行为将被认定为 **垃圾贡献**：

- **小题大做**：用 AI 生成数千字的长篇论证，实际变更却微乎其微——例如整篇 issue 长篇大论，diff 仅仅是把某个依赖从 1.1.1 升级到 1.1.2；
- **虚张声势**：提交宣称"实现了某某模块"的 feat PR，实际改动只是新建了一个文件，里面只有一行注释"待实现某某"；
- **未经审查的批量产出**：明显由 AI 批量生成、与项目实际状况脱节的 issue / PR 文本；
- **任何形式的上传即走**：提交者显然没有读过项目、没有运行过代码，仅把 AI 输出当作刷存在感的耗材。

对于垃圾贡献，我们将 **直接关闭，并回复解释一次**，指出其违反本准则的具体条款。若该账号**此后仍持续提交此类贡献**，我们将 **不再回复**，直接关闭处理。评审者没有义务替你筛选、订正或总结 AI 的产出——这份义务在你这边。

在以下两种彼此独立的情形下，我们将直接拉黑该账号，不再接受其任何贡献：

1. **多次违反本准则。** 屡次违规（无论是否借助 AI）表明协作无法继续，与账号背后是真人还是程序无关；
2. **认定为 AI Agent 自动控制的账号。** 即该账号的贡献行为并非由自然人负责和审查，而是由自动化程序代为进行。该认定由维护者基于账号的公开行为综合判断作出。一般有以下判断标准：
 - 无法解释代码。
 - 拒绝披露自然人贡献者。
 - 持续无视评审。
 - 贡献描述中出现明显的LLM提示词或回答。

一般情况下，我们会先对违规的贡献进行提示、警告，多次警告无效后，我们才会进行拉黑。

如果您认为我们错误地拉黑了您的账号，请允许我们向您致歉，您可以通过邮件联系我们申诉和复核，以便恢复您的账号。但注意，如果我们复核后依然认为您违反准则，我们将拒绝您后续的所有申诉，这意味着每个账号 **有且仅有一次** 申诉机会。

简而言之：我们欢迎 AI 作为辅助工具，但 **必须由自然人负责**。由自然人拥有并负责一项变更，核验其质量与来源；AI 的角色仅限于 **辅助编写**，随后由该自然人核查、修正并为之担保。自然人缺位的贡献，将被视为没有贡献者。

* * *

## 5. 贡献描述真实性要求

贡献的描述（issue、PR、提交信息、安全公告等）必须 **属实**，并与实际贡献内容 **一致**。

- **描述与变更一致。** 标题和正文描述的内容，必须能在 diff 中逐条找到对应。不得宣称完成了某模块、某特性、某修复，实际却没有完成或只完成了一部分；完成度有限就请如实写明进度与剩余工作，我们接受诚实的半成品（见第 6 节关于 draft PR 的说明），不接受夸大的完成声明。
- **不得提交虚假信息。** 不得捏造复现步骤、编造测试结果、虚构讨论依据，或陈述任何你未验证过的事实。
- **安全与漏洞声明须提供可核验的证据。** 报告安全问题时，需要提供具体的触发路径或复现条件，如存在 CVE ，则请给出编号、受影响版本，并区分以下两种情况：
  - **项目自身代码** 存在漏洞；
  - 某个 **依赖** 存在 CVE，但本项目的 **核心链路并不具备触发该漏洞的条件**。
  后一种情况不构成"项目存在该漏洞"，将其表述为项目漏洞属于虚假声明。我们对真实的安全问题非常重视，但不会为夸大或捏造的安全声明花费排查时间——此类声明将按垃圾贡献处理（见第 4.1 节），情节严重的账号将被拉黑。若无法证明本项目默认配置或核心链路可触发，不得声称项目自身存在该漏洞；但欢迎提交依赖升级建议，维护者会评估。
- **失实描述的处理。** 描述与实际内容不符的 issue / PR 将被关闭；明知故犯的虚假陈述属于严重违反本准则，涉事账号将被拉黑。

* * *

## 6. Pull Request 与 issue 规范

### 6.1 功能性贡献：先沟通，再动手

- 我们 **支持并欢迎** 功能性贡献——新特性、新插件、新命令、行为树 / 管线扩展、协议适配等。
- 但我们建议 **先与我们沟通，再投入实现**：开 issue 说明动机与方案，通过其他渠道与维护者对齐，或者先提 **draft PR** 作为讨论载体。一次简短的方案确认，可以避免双方白费功夫，也避免你的 PR 因方向不被接受而被整体推翻。

### 6.2 许可证与来源合规

- **严禁引入** 许可证不兼容、存在许可证争议，或在无明确授权依据下受私有版权保护的代码。这一点对所有人都是强制要求。
- 当 AI 参与编写代码时， **提交开发者** 有责任 **逐项核验** 每一份贡献代码的来源与许可证。如有疑问，先沟通再提交。
- 本项目基于 MIT License 授权；收到的贡献在同一许可条款下纳入。

### 6.3 建设性协作

协作需要双方共同投入才能运转良好。请：

- **提出真诚、有善意的贡献。** 不要只做单向的索取——要求他人投入大量工作，却没有任何关于你自身愿意参与或迭代的信号。
- **尊重维护者的时间。** 他们在自己的节奏下评审，请避免催促式的要求与理所当然的态度。
- **以证据与尊重进行沟通。** 请用事实、参考资料与可测量的结果支撑你的提案，把分歧当作需要解决的技术问题，而非个人冲突。
- **表现可预期、乐于合作。** 反复无常、无端对抗或拒绝建设性沟通，会让协作代价高昂。维护者保留对此类行为不予回应的权利。如果你受阻或有疑问，请说明你已尝试过的做法并提出具体问题，不要在没有明确依据的情况下升级对抗。

### 6.4 禁止歧视性或攻击性言论

- 在 issue、pull request、评审或任何渠道中， **严禁任何形式的歧视、骚扰或攻击**。请以尊重的态度对待每一位贡献者。

### 6.5 面向维护者的沟通规范

- issue、pull request、评审评论等 **面向维护者** 的沟通内容，以可读性与简洁性为主：请使用准确、平实的语言（中文即可，不强求英文）； **请勿使用 emoji**——标题中一律严禁，正文中请勿使用。理由：emoji 表达的是情绪而非信息，对描述问题或变更没有信息增益，还会降低标题与正文的可读性与可检索性。
- 项目内 **面向用户** 的内容（聊天文案、技能、插件回复等）不受此限，请参见第 2.5 节。

### 6.6 提交前先检索

- **在新建 issue 之前，请先检索** 是否已有重复或相近的 issue。
- 如果既有 issue 只覆盖了你的问题的一部分，可再提一个新的 issue，并 **与旧 issue 交叉引用**，说明两者差异，以及为何需要单独提出。

* * *

## 7. 代码评审与合并流程

- **欢迎请求评审。** 开 PR 时，请附上一段简短说明：改了什么、为什么改，并关联相关 issue。
- **评审要说具体。** 反馈应指出具体的行或行为，陈述问题，并给出方向建议。尽量用提问与建议，而非命令。
- **作者要回应并更新。** PR 作者应当回复评论并推送更新的提交。若同意建议则采纳；若不同意，请用事实说明理由。
- **小而聚焦的 PR 合并更快。** 保持 diff 聚焦，便于评审者推理。
- **合入前 CI 必须通过。** 包括 `go vet`、`go build` 与完整的 `go test ./...`；涉及前端的改动需保证 `manager/` 可构建。
- **评审与合并由人来完成。** 变更需经核心维护者评审后合入。AI 工具不评审、不合入；评审始终由自然人对自然人署名的贡献进行端到端的把关。
- **合并纪律。** 除非是已与维护者明确商定的琐碎修复，否则不要在无第二人评审的情况下合入自己的 PR。按项目既有约定采用 squash 或 rebase，以保持提交历史整洁。

感谢你为 lanmei-dream 做贡献。我们珍视每一位认真、诚实、尊重他人时间的协作者。